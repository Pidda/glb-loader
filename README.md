# Modelviewer with React + TypeScript

A simple 3D model viewer for three preset glb models

Developed in Visual Code using Typescript, React and React Three Fiber (I think).
Target browser: Chrome 

# Installation
```
git clone https://github.com/Pidda/glb-loader.git
cd glb-loader
npm install
npm run dev
```